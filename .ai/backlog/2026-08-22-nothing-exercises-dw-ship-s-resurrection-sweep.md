---
created: 2026-08-22
source: date-prefix-the-solo-lane-slugs
---

# `dw-ship`'s resurrection sweep is untested, and it only looks at `.ai/work/`

Two things about the same step, which ship together — same skill, one version bump:

- **Nothing exercises it**, though its match is now bare slug **plus** `unclaimed`/`shaping` against a
  twin reading `landed:` — a rule that exists so a reused slug cannot delete live work. Blocked on
  deciding what can test skill prose at all.
- **It sweeps work folders only.** A shaping commit also carries `.ai/backlog/` entries, and one
  absorbed during the change comes back as queued work — observed on this very ship, which put the
  backlog at its cap. The sweep should match those on the bare slug too.

Detail: `.ai/archive/2026-08-22-date-prefix-the-solo-lane-slugs`.
