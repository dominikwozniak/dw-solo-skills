# `.ai/archive/` — landed changes, kept as history

Change docs moved here by `dw-land` at close: `status: landed`, plus `landed:` date and `pr:` in
the frontmatter. **History, not guidance** — nothing reads this folder to decide anything; the
durable layer lives in `docs/decisions/`, `CONTEXT.md` and `## Gotchas` in `CLAUDE.md`. Backlog
entries may point here for a change's findings (its `## Notes`).
