---
created: 2026-08-22
source: date-prefix-the-solo-lane-slugs
---

# Nothing exercises `dw-ship`'s resurrection sweep, the one step that runs `git rm -r`

Its match is now bare slug **plus** `unclaimed`/`shaping` against a twin reading `landed:` — a rule
that exists so a reused slug can't delete live work, and that nothing verifies but reading. Blocked on
deciding what can test skill prose at all. Detail:
`.ai/archive/2026-08-22-date-prefix-the-solo-lane-slugs`.
